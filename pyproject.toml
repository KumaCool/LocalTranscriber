[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "local-transcriber"
dynamic = ["version"]
description = "Offline CPU speech transcription with anonymous speaker diarization"
readme = "README.md"
requires-python = ">=3.11,<3.12"
license = "MIT"
license-files = ["LICENSE"]
authors = [
  { name = "Shunnketu Kuma" },
]
keywords = [
  "speech-to-text",
  "transcription",
  "speaker-diarization",
  "funasr",
  "sensevoice",
  "offline",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Operating System :: MacOS :: MacOS X",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Topic :: Multimedia :: Sound/Audio :: Speech",
]
dependencies = [
  "cryptography==46.0.3; sys_platform == 'darwin' and platform_machine == 'x86_64'",
  "funasr==1.3.30",
  "llvmlite==0.45.1; sys_platform == 'darwin' and platform_machine == 'x86_64'",
  "modelscope==1.39.0",
  "numba==0.62.1; sys_platform == 'darwin' and platform_machine == 'x86_64'",
  "numpy==1.26.4; sys_platform == 'darwin' and platform_machine == 'x86_64'",
  "psutil==7.2.2",
  "torch==2.2.2; sys_platform == 'darwin' and platform_machine == 'x86_64'",
  "torch==2.10.0; sys_platform != 'darwin' or platform_machine != 'x86_64'",
  "torchaudio==2.2.2; sys_platform == 'darwin' and platform_machine == 'x86_64'",
  "torchaudio==2.10.0; sys_platform != 'darwin' or platform_machine != 'x86_64'",
  "transformers==4.46.3; sys_platform == 'darwin' and platform_machine == 'x86_64'",
]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cpu" }
torchaudio = { index = "pytorch-cpu" }

[project.scripts]
local-transcriber = "local_transcriber.cli:main"

[project.urls]
Homepage = "https://github.com/KumaCool/LocalTranscriber"
Repository = "https://github.com/KumaCool/LocalTranscriber.git"
Issues = "https://github.com/KumaCool/LocalTranscriber/issues"
Documentation = "https://github.com/KumaCool/LocalTranscriber#readme"

[dependency-groups]
dev = [
  "pytest==9.0.2",
  "ruff==0.12.7",
]

[tool.hatch.build.targets.wheel]
packages = ["src/local_transcriber"]

[tool.hatch.build.targets.sdist]
include = [
  "/CHANGELOG.md",
  "/CONTRIBUTING.md",
  "/LICENSE",
  "/README.md",
  "/SECURITY.md",
  "/src",
]

[tool.hatch.build.targets.wheel.force-include]
"CHANGELOG.md" = "CHANGELOG.md"
"LICENSE" = "LICENSE"
"README.md" = "README.md"

[tool.hatch.version]
path = "src/local_transcriber/__init__.py"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
