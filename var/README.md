# LocalTranscriber `var/`

本目录保存本机运行数据，不是源码：

- `cache/`：模型缓存；
- `input/`：授权输入媒体；
- `work/`：可清理的中间文件；
- `output/`：转写产物；
- `acceptance/`：本地详细验收证据。

这些目录的内容默认被 `.gitignore` 排除，只保留目录占位文件。未经明确授权，不提交原始录音或转写内容。
