# Source package

`local_transcriber` 是 LocalTranscriber 的可安装 Python 包，包含：

- CLI 命令与参数处理；
- 环境和媒体探测；
- 固定模型清单与显式模型预取；
- FunASR 推理、任务状态和单 worker 控制；
- 规范结果模型及 JSON、Markdown、TXT、SRT 导出。

公共安装、使用和开发说明见仓库根目录的 [`README.md`](../README.md)。
